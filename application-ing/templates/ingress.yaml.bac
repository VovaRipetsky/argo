apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: ingress-paths

spec:
  rules:
  - host: app.vova.click
    http:
      paths:
        - backend:
           serviceName: backend-service
           servicePort: 8080

